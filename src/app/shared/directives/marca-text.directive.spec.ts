import { MarcaTextDirective } from './marca-text.directive';

describe('MarcaTextDirective', () => {
  it('should create an instance', () => {
    const directive = new MarcaTextDirective();
    expect(directive).toBeTruthy();
  });
});
